# Example Codex Note Plugin

This is a minimal plugin scaffold. It demonstrates the expected folder shape for a plugin that ships a bundled skill.

## Contents

- `.codex-plugin/plugin.json` - plugin manifest
- `skills/note-capture/SKILL.md` - bundled example skill

## Use

Copy this folder when starting a new plugin, then rename the plugin and replace the bundled skill.
